# Testing

Currently, there are no automated test suites configured for this project.

## CI Workflow
The CI pipeline (`.github/workflows/ci.yml`) verifies that the application builds successfully via `npm run build`.

## Future Improvements
- Implement unit tests for utility functions using Vitest.
- Implement component tests for React components.
- Implement e2e tests for critical user journeys (e.g., Auth, Map rendering).
