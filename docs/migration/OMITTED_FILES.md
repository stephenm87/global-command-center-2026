# Omitted Files

The following files were deliberately ignored or omitted during migration:
- Local `.env` files containing actual secrets.
- `node_modules/` directory.
- `dist/` built assets directory.
- Operating system files (`.DS_Store`).
